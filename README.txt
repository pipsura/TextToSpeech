Before running: 
    Change the value "ELEVENLABS_API_KEY" in the .env file to your personal elevenlabs api key. 
    You can also change the voice used.

To run:
    Ensure you have navigated to the correct folder (...\TextToSpeech)
    Run the command node app.js
    Navigate to localhost:3000